@model TinyCityCardGame_online.Models.ErrorViewModel
@{
    ViewData["Title"] = "Ошибка";
}

<div class="error-container">
    <h1 class="text-danger">⚠️ Ошибка</h1>
    <h2 class="text-danger">Произошла ошибка при обработке вашего запроса</h2>
    
    @if (Model?.ShowRequestId ?? false)
    {
        <div class="request-info">
            <p>
                <strong>Идентификатор запроса:</strong> 
                <code>@Model?.RequestId</code>
            </p>
        </div>
    }

    <div class="development-mode">
        <h3>Режим разработки</h3>
        <p>
            Переключение на среду <strong>Development</strong> отобразит подробную информацию об ошибке.
        </p>
        <p class="warning">
            <strong>Внимание:</strong> Режим разработки не должен быть включен в production.
            Это может привести к отображению конфиденциальной информации об исключениях.
        </p>
        <p>
            Для локальной отладки установите переменную окружения 
            <strong>ASPNETCORE_ENVIRONMENT</strong> в значение <strong>Development</strong>
            и перезапустите приложение.
        </p>
    </div>
</div>

<style>
    .error-container {
        max-width: 800px;
        margin: 50px auto;
        padding: 40px;
        background: #fdf5e6;
        border: 8px solid #8b4513;
        border-radius: 15px;
    }
    
    .request-info {
        background: rgba(139, 69, 19, 0.1);
        padding: 15px;
        border-radius: 5px;
        margin: 20px 0;
    }
    
    .development-mode {
        margin-top: 30px;
        padding-top: 30px;
        border-top: 2px dashed #8b4513;
    }
    
    .warning {
        color: #8b0000;
        font-weight: bold;
    }
</style>